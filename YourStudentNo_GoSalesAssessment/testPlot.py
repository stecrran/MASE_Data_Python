import matplotlib.pyplot as plt
def main():

    

    """
    # Sample data
    x = [1, 2, 3, 4, 5]
    y = [10, 20, 25, 30, 35]

    # Using plt.xticks()
    plt.plot(x, y)
    plt.xticks([1, 2, 3, 4, 5], ['One', 'Two', 'Three', 'Four', 'Five'])  # Custom tick labels
    plt.show()

    # Using ax.set_xticks() and ax.set_xticklabels()
    fig, ax = plt.subplots(figsize=(12,8))
    #ax.plot(x, y)
    #ax.set_xticks([1, 2, 3, 4, 5])  # Set tick positions
    #ax.set_xticklabels(['One', 'Two', 'Three', 'Four', 'Five'])  # Set custom labels

    test = ['ham', 'cheese']
    othertest = ['orange', 'lemon']
    ax.bar(x,y, label='Total Profit', color='blue')
    plt.show()
    """

if __name__=="__main__":
    main()